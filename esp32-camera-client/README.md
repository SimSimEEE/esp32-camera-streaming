# ESP32 Camera Client

ESP32-CAM 실시간 스트리밍을 위한 웹 클라이언트입니다.

## 📋 구성

- `index.html` - 메인 HTML 페이지
- `styles.css` - 스타일시트
- `app.js` - 클라이언트 애플리케이션 로직

## 🚀 사용 방법

### 방법 1: 브라우저에서 직접 열기

```bash
open index.html
```

또는 브라우저에서 파일을 직접 드래그&드롭

### 방법 2: HTTP 서버 사용 (권장)

#### Python 간단한 HTTP 서버

```bash
# Python 3
python3 -m http.server 8080

# Python 2
python -m SimpleHTTPServer 8080
```

그 후 브라우저에서 `http://localhost:8080` 접속

#### Node.js http-server

```bash
# http-server 설치 (한 번만)
npm install -g http-server

# 서버 시작
http-server -p 8080
```

#### VS Code Live Server

VS Code에서 Live Server 확장 프로그램 설치 후:
- `index.html` 파일 우클릭
- "Open with Live Server" 선택

## ⚙️ 설정

### WebSocket 서버 주소 변경

`app.js` 파일의 `wsUrl`을 수정하세요:

```javascript
// 로컬 서버
this.wsUrl = 'ws://localhost:8887/viewer';

// 원격 서버
this.wsUrl = 'ws://192.168.0.100:8887/viewer';
```

## 🎮 사용법

1. **서버 시작**: ESP32 Camera Server가 실행 중인지 확인
2. **웹 페이지 열기**: `index.html`을 브라우저에서 열기
3. **연결**: "연결" 버튼 클릭
4. **ESP32 연결**: ESP32-CAM이 서버에 연결되면 자동으로 스트림 시작
5. **종료**: "연결 해제" 버튼으로 연결 종료

## 📊 화면 정보

### 상단 상태 표시
- 🟢 녹색: 서버 연결됨
- 🔴 빨간색: 연결 끊김

### 정보 패널
- **FPS**: 초당 프레임 수
- **해상도**: 영상 해상도 (예: 640x480)
- **수신 데이터**: 총 수신한 데이터량 (KB)
- **프레임 수**: 총 수신한 프레임 수

### 연결 로그
- 실시간 연결 상태 및 이벤트 로그
- 최근 50개 로그 항목 표시

## 🌐 브라우저 호환성

- Chrome/Edge (권장)
- Firefox
- Safari
- Opera

**최소 요구사항**: WebSocket 및 Canvas API 지원

## 🔍 문제 해결

### 연결이 안 될 때
1. WebSocket 서버가 실행 중인지 확인
2. `app.js`의 서버 주소가 올바른지 확인
3. 방화벽에서 포트가 열려있는지 확인
4. 브라우저 콘솔(F12)에서 에러 확인

### 영상이 표시되지 않을 때
1. ESP32-CAM이 서버에 연결되었는지 확인
2. 서버 로그에서 프레임 수신 여부 확인
3. 브라우저 콘솔에서 JPEG 디코딩 에러 확인

### 성능이 낮을 때
1. 네트워크 상태 확인
2. ESP32의 프레임 레이트 조정
3. ESP32의 영상 품질 조정
4. 다른 브라우저 탭 닫기

## 🎨 커스터마이징

### 스타일 변경

`styles.css`를 수정하여 색상, 레이아웃 등을 변경할 수 있습니다:

```css
/* 그라데이션 색상 변경 */
background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
```

### 기능 추가

`app.js`의 `CameraViewer` 클래스를 확장하여 기능을 추가할 수 있습니다:

```javascript
class CameraViewer {
    // 스냅샷 저장 기능
    takeSnapshot = () => {
        const dataUrl = this.canvas.toDataURL('image/jpeg');
        const link = document.createElement('a');
        link.download = `snapshot_${Date.now()}.jpg`;
        link.href = dataUrl;
        link.click();
    };
}
```

## 📱 모바일 지원

이 클라이언트는 반응형 디자인으로 모바일 브라우저에서도 작동합니다:
- 터치 스크린 지원
- 세로/가로 방향 자동 조정
- 모바일 최적화 레이아웃

## 📝 라이선스

Copyright (C) 2026 LemonCloud Co Ltd. - All Rights Reserved.
